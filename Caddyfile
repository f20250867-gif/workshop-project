{$SITE_ADDRESS} {
    encode gzip

    header Strict-Transport-Security "max-age=31536000; includeSubDomains"

    handle_path /static/* {
        root * /srv/static
        file_server
    }

    reverse_proxy web:8000
}
